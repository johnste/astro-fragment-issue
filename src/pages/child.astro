with fragment:<br/>
{["hello", "goodbye"].map((value, i) => (
<>
    <strong>{i}: {value}</strong><br/>
</>
))}
<br/>
without fragment:<br/>
{["hello", "goodbye"].map((value, i) => (

    <strong>{i}: {value}</strong>

))}